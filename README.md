# wordcount-cli

Tiny Python CLI that prints line, word, and character counts for a text file.

## Usage
```bash
python wordcount.py path/to/file.txt

### 7.3 Edit `.gitignore`
Open `.gitignore`, paste, save:

```gitignore
# Python
__pycache__/
*.pyc

# Virtual envs
.venv/
venv/

# OS / editors
.DS_Store
.vscode/